# Examples

This page lists runnable examples included with the repository.

- `examples/generate_example_data.py` — generates `examples/synthetic_lookup.csv` and `examples/predict_inputs.csv` used by the quickstart.
- `examples/config_fast.toml` — small config for quick local experiments.

Reproducing the README quickstart is the recommended smoke test for a local setup.
